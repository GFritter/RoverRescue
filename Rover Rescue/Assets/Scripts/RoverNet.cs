using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoverNet : MonoBehaviour
{
    Function move;

    // Start is called before the first frame update
    void Start()
    {
        //cria a rede e seta as  fun;oes de callback para seus respectivos lugares

        
    }

    // Update is called once per frame
    void Update()
    {
        //trata os inputs q vao adicionar as marcas
        
    }
}
